```bash

# Python workouts:

This repository contains all of my Python programs and scripts

a) Boto3_Scripts
b) General Python programs
c) AWS_CLI scripts
d) Pyscript
e) API

# File Handling:

"r" - Read - Default value. Opens a file for reading, error if the file does not exist.
"a" - Append - Opens a file for appending, creates the file if it does not exist
"w" - Write - Opens a file for writing, creates the file if it does not exist.
"x" - Create - Creates the specified file, returns an error if the file exists.

# Pyscript:

PyScript allows us to either embedded Python code in HTML or link to a Python file and the code will run in the browser. 
We don't need to run Python in backend.
