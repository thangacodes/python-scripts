import time
import sys
import os
a ="Python is a very friendly programming language"
print(f"Value of a is: {a}")
time.sleep(3)
folder_names = ["/tmp/", "/home/ec2-user/"]
print(f"You are checking the files on folders like: {folder_names}")
for folder in folder_names:
    files = os.listdir(folder)
    for file in files:
        print(f"The files in the folder {folder} are: {file}")
Worldcup_semi_finalist = ["INDIA","SA","AUSTRALIA","NEWZEALAND"]
for j in Worldcup_semi_finalist:
  print(f"These are the seminfinalist team: {j}")

